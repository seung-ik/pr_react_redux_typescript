interface initialState{

}
const initialState:initialState={

}

const PokenmonReducer = (state=initialState,action:any)=>{
  switch(action.type){

    default:
      return state
  }
}

export default PokenmonReducer